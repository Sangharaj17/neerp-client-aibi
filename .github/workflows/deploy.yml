name: Build and Deploy Client to VPS

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  # ===== BUILD BACKEND JAR =====
  build-backend:
    runs-on: ubuntu-latest
    name: Build Backend JAR
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: ☕ Set up Java 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven

      - name: 📦 Build Backend JAR
        working-directory: backend
        run: ./mvnw clean package -DskipTests

      - name: 📤 Upload JAR artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-jar
          path: backend/target/*.jar
          retention-days: 1

  # ===== BUILD FRONTEND ON GITHUB ACTIONS (NOT VPS!) =====
  build-frontend:
    runs-on: ubuntu-latest
    name: Build Frontend
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: 📥 Install dependencies
        working-directory: frontend
        run: npm ci

      - name: 📝 Create .env.production
        working-directory: frontend
        run: |
          cat > .env.production << EOF
          NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
          EOF

      - name: 🔨 Build Next.js
        working-directory: frontend
        run: npm run build

      - name: 📦 Package standalone build
        working-directory: frontend
        run: |
          # Copy static files and public folder to standalone
          cp -r public .next/standalone/public
          cp -r .next/static .next/standalone/.next/static

      - name: 📤 Upload frontend artifact
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: frontend/.next/standalone
          retention-days: 1

  # ===== DEPLOY TO VPS =====
  deploy:
    runs-on: ubuntu-latest
    needs: [build-backend, build-frontend]
    name: Deploy to VPS
    steps:
      - name: 📥 Download backend JAR
        uses: actions/download-artifact@v4
        with:
          name: backend-jar
          path: ./backend-jar

      - name: 📥 Download frontend build
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: ./frontend-build

      - name: 🔑 Setup SSH key
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.VPS_SSH_KEY }}" > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa
          ssh-keyscan -p ${{ secrets.VPS_PORT }} ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: 🚀 Deploy Frontend via rsync
        run: |
          rsync -avz --delete \
            -e "ssh -p ${{ secrets.VPS_PORT }}" \
            ./frontend-build/ \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/var/www/neerp-client-aibi/frontend/

      - name: 📤 Deploy Backend JAR
        run: |
          scp -P ${{ secrets.VPS_PORT }} \
            ./backend-jar/*.jar \
            ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}:/var/www/neerp-client-aibi/backend/target/

      - name: 🔄 Restart Services
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}
          script: |
            echo "🔄 Restarting Frontend..."
            cd /var/www/neerp-client-aibi/frontend
            pm2 delete neerp-client-frontend 2>/dev/null || true
            PORT=3001 pm2 start server.js --name neerp-client-frontend
            
            echo "🔄 Restarting Backend..."
            pm2 restart neerp-client-backend
            
            echo "✅ Deployment Complete!"
            pm2 status

      - name: 📊 Deployment Status
        if: always()
        run: |
          if [ "${{ job.status }}" == "success" ]; then
            echo "✅ Deployment successful!"
          else
            echo "❌ Deployment failed!"
          fi