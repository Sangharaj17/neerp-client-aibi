spring.application.name=neerp

server.port=8080

# --------- File logging
logging.file.name=logs/app.log
logging.level.root=INFO
# Set custom log levels per package if needed
logging.level.com.aibi.neerp=DEBUG



# --------- Database Configuration ---
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.properties.hibernate.dialect =org.hibernate.dialect.PostgreSQLDialect



# --------- Fallback DB (just to let Spring Boot start and initialize JPA infrastructure)
spring.datasource.url=jdbc:postgresql://localhost:5432/fallback_client_db
spring.datasource.username=postgres
spring.datasource.password=root



# --------- Naming Strategy
# Use Spring's snake_case naming so camelCase fields map to snake_case columns
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.CamelCaseToUnderscoresNamingStrategy



spring.sql.init.mode=never
spring.jpa.show-sql=true
# Prevent Hibernate from altering existing schemas (for ddl = none) (avoid cast errors on startup)
# ? RECOMMENDATION: Set DDL generation to 'none' for multi-tenant systems.
spring.jpa.hibernate.ddl-auto=update

# --------- Email / OTP configuration
resend.api.key=re_LfbCuXoi_ApibLmfkTc8nHqHikjkXR7BZ
resend.from.email=noreply@email.aibistreet.com
password.reset.internal.secret=Ua1iZ7h4Q2mJ5sR8nX0pV6tW3kE9bLc

# ========================================
# EMAIL CONFIGURATION - Gmail Example
# ========================================

# SMTP Server Configuration
spring.mail.host=smtp.gmail.com
spring.mail.port=587

# Your Gmail credentials
spring.mail.username=murudkardipali123@gmail.com
spring.mail.password=grqpwnhwulbfagql

# SMTP Properties
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
#spring.mail.properties.mail.debug=true

# Connection timeout
spring.mail.properties.mail.smtp.connectiontimeout=30000
spring.mail.properties.mail.smtp.timeout=30000
spring.mail.properties.mail.smtp.writetimeout=30000

# Default encoding
spring.mail.default-encoding=UTF-8


# --------- Base directory for photos and signatures ----------------------
#file.upload.base-path=frontend/app/api/uploads
##file.upload.base-path=D:/neerp-aibi-production/neerp-client-aibi/frontend/app/api/uploads
#file.max.size=5242880

# --------- Multipart configuration (IMPORTANT) ----------
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=100MB
spring.servlet.multipart.file-size-threshold=2KB

# --------- Your custom validation -----------------------
file.max.size=52428800
#file.upload.base-path=uploads
file.upload.base-path=uploads
app.backend.url=http://localhost:${server.port}


# --------- DevTools Configuration ---
# Control auto-restart behavior during development

# Option 1: Disable restart completely (uncomment to disable)
# spring.devtools.restart.enabled=false

# Option 2: Set trigger file - only restart when this file is modified
# This gives you manual control over restarts
# spring.devtools.restart.trigger-file=.reloadtrigger

# Option 3: Exclude specific paths from triggering restart
spring.devtools.restart.exclude=logs/**,static/**,public/**,*.log


